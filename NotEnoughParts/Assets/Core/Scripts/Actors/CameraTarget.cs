using UnityEngine;

namespace CGL.Actor
{
	// marks a transform as the target point for the camera to follow.
	// attach to a child object on the player (e.g. chest, head bone).
	public class CameraTarget : MonoBehaviour { }
}